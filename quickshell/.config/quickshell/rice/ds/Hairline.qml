import QtQuick

// Explicit divider — a last resort after air and section labels (Foundation).
// Allowed uses: popup zone separators, section splits in dense lists.
Rectangle {
    implicitHeight: 1
    height: 1
    color: Tokens.hairline
}
