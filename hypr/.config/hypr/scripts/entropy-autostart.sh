#!/usr/bin/env bash
# Entropy autostart — DISABLED.
# On Hyprland/Wayland the AppImage often hangs ("not responding") at login.
# EN/RU firmware sync is handled by eh-layout-sync.sh instead.
# Launch Entropy manually when you need the GUI: `entropy` or the desktop entry.
exit 0
