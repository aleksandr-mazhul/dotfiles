#!/usr/bin/env bash
# Deprecated: stack toggle is handled in binds.lua via hl Group:add.
# Kept so old keybind paths don't hard-fail if something still calls it.
exec ~/.config/hypr/scripts/group-notify.sh "use Alt+G"
