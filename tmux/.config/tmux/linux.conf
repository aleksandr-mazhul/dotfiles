set -g @copy_command 'wl-copy'

# Prefix notes (Linux):
#   C-b  — primary (mouse macro pad "service mode")
#   C-a  — secondary (legacy Mac muscle memory)
# Kitty maps Super/Ctrl+Shift+[ ] and Ctrl+Page_Up/Down → C-b p/n for window switch.
