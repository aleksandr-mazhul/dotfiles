// Generated by theme-ssot — do not edit by hand
// NOTE: do not use QML property names starting with "on" (reserved for signal handlers)
pragma Singleton
import QtQuick

QtObject {
    readonly property color background: "{{ background }}"
    readonly property color surface: "{{ surface }}"
    readonly property color surface_container: "{{ surface_container }}"
    readonly property color surface_container_high: "{{ surface_container_high }}"
    readonly property color surface_variant: "{{ surface_variant }}"
    readonly property color primary: "{{ primary }}"
    readonly property color secondary: "{{ secondary }}"
    readonly property color text: "{{ on_surface }}"
    readonly property color text_muted: "{{ on_surface_variant }}"
    readonly property color text_on_primary: "{{ on_primary }}"
    readonly property color outline: "{{ outline }}"
    readonly property color outline_variant: "{{ surface_variant }}"
    readonly property color error: "{{ error }}"
    // Chrome tokens — bars/widgets (tmux / starship / QS islands)
    readonly property color chrome_highlight_bg: "{{ chrome_highlight_bg }}"
    readonly property color chrome_highlight_fg: "{{ chrome_highlight_fg }}"
    readonly property color chrome_panel_bg: "{{ chrome_panel_bg }}"
    readonly property color chrome_panel_fg: "{{ chrome_panel_fg }}"
    readonly property color chrome_muted_bg: "{{ chrome_muted_bg }}"
    readonly property color chrome_muted_fg: "{{ chrome_muted_fg }}"
    readonly property string font_mono: "{{ font_mono }}"
    readonly property string font_ui: "{{ font_ui }}"
}
