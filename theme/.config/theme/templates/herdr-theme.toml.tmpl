# Generated by theme-ssot — do not edit by hand
onboarding = false

[theme]
name = "vesper"

[theme.custom]
panel_bg = "reset"
accent = "{{ primary }}"
green = "{{ secondary }}"
yellow = "{{ tertiary }}"
red = "{{ error }}"
blue = "{{ primary }}"
teal = "{{ secondary }}"
peach = "{{ outline }}"
mauve = "{{ secondary }}"
text = "{{ on_surface }}"
subtext0 = "{{ on_surface_variant }}"
overlay0 = "{{ surface_variant }}"
overlay1 = "{{ outline }}"
surface0 = "{{ surface_container }}"
surface1 = "{{ surface_container_high }}"
surface_dim = "{{ surface_variant }}"

[ui]
accent = "{{ primary }}"
