# Generated by theme-ssot — include from kitty.conf
# Window chrome is achromatic glass (ADR-0006 / slice 9).
# Do not use palette primary / on_primary / surface_container /
# on_surface_variant — those restored sand invert + milk chips.
# Idle bg tracks {{ background }} so chips use background_opacity (Kovid:
# same hex as terminal bg). Active #2d282b is raisedStrong pre-composited
# (#181316 + white 0.06), not a palette tint. Recompute if background changes.
active_tab_foreground   #f0f0f0
active_tab_background   #2d282b
inactive_tab_foreground #9e9e9e
inactive_tab_background {{ background }}
tab_bar_background      none
