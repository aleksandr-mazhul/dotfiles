# Generated by theme-ssot — include from kitty.conf
# Glass chrome. Do not restore palette primary on tabs. Idle fill tracks cell background
# so it shares the translucent plate (kitty cannot none tab fills).
# Active #2d282b = background #181316 + white 0.06 (material.raised.strong).
# Recompute if background changes (template engine has no arithmetic).
active_tab_foreground   #f0f0f0
active_tab_background   #2d282b
inactive_tab_foreground #9e9e9e
inactive_tab_background #181316
tab_bar_background      none
