# Arch google-chrome-stable wrapper reads this file (one flag set per line).
# Always reopen the previous window(s)/tabs, including after an unclean exit
# (Hyprland logout / power-off leaves profile.exit_type=Crashed).
--restore-last-session
